CREATE TABLE INFOEX(
NAME        VARCHAR2(20),
ID              CHAR(14) unique not null,
TEL             NUMBER(13)primary key,
SEX             CHAR(4),
AGE            NUMBER(3),
HOME         VARCHAR2(10)
);

SELECT
    *
FROM infoex;